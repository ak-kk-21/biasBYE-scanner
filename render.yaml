services:
  - type: web
    name: biasbye-scanner
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt -r requirements-api.txt
    startCommand: uvicorn api:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PORT
        value: 10000